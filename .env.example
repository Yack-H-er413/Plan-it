# Generate with: npx auth secret
AUTH_SECRET=

# Google OAuth (Google Cloud Console → Credentials → OAuth client ID)
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Optional (useful on some hosts like Vercel preview deployments)
# AUTH_TRUST_HOST=true
